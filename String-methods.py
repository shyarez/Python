name = input("Enter your name: ")

# result = len(name)             # len - calculates length of string including whitespace
# result = name.find("M")        # find - returns the 1st occurance of a given chara
# result = name.rfind("h")       # rfind - (r is reverse) - returns the last occurance of a given chara
# result = name.capitalize()     # capitalize - capitalizes the 1st letter of your string
# result = name.upper()          # upper - makes all lower-case letters into uppercase
# result = name.lower()          # lower - makes all upper-case letters into lowercase
# result = name.isdigit()        # isdigit - returns T or F based on if it finds any digits, 
                                 # returns T only if string has ONLY DIGITS
                              
# result = name.isalpha()        # isalpha - returns T or F based on if it finds any alphabetical charas, 
                                 # returns T only if string has ONLY ALPHABETS(excluding whitespaces & digits)
# result = name.count("-")       # count - counts the number of times a character has occured
# result =  name.replace("-"," ")  # replace - replaces current chara w/ something else u want
                  
# print(result)

# TO ACCESS the comprehensive list of all strings available to you:
# use: print(help(str))
